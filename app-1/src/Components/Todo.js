import React from 'react'

const Todo = (props) => {
    return <h1>{props.item}</h1>
}
export default Todo

// This file is a functional component. This will only display looks of the component style that we want.